<ul class="nav navbar-nav">
    <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Clouds <span class="caret"></span></a>
        <ul class="dropdown-menu">
            {{#each this}}
                <li class="dropdown-header">{{name}}</li>
                {{#each projects}}
                    {{#each regions}}
                        {{#if enabled}}
                            <li><a href="#"><span class="glyphicon glyphicon-check"></span> {{../../name}} - {{name}}</a></li>
                        {{else}}
                            <li><a href="#"><span class="glyphicon glyphicon-unchecked"></span> {{../../name}} - {{name}}</a></li>
                        {{/if}}
                    {{/each}}
                {{/each}}
            {{/each}}
        </ul>
    </li>
</ul>